# Kopiere diese Datei zu .env und passe die Werte an

# JWT signing secret – beliebiger langer zufälliger String
JWT_SECRET=ersetze_mich_mit_einem_langen_geheimen_string

# Admin-Passwort (wird beim ersten Start gehasht in die DB geschrieben)
ADMIN_PASSWORD=admin123

# Port des Backends
PORT=3000
